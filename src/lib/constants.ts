/**
 * SITE_CONFIG
 * Central source of truth for site-wide branding, SEO, and domain settings.
 * No more hardcoded domains in build scripts!
 */

import SITE_CONFIG from './site-config.json';
export { SITE_CONFIG };

export const CONTACT_INFO = {
    supportEmail: 'support@coreload.app',
};
